# Next.js

- Use the App Router under `src/app`.
- Prefer Server Components and server data loading.
- Use Client Components only where needed.
- Keep layouts boring and predictable.
- Generate useful metadata for public pages.
- Keep route handlers small and move shared logic to `src/lib`.
